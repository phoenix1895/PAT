思路：
1.定义静态链表，其中flag表示结点在链表中是否出现，flag 为 false表示无效节点。
2.初始化flag，令flag为false，表示初始状态均为无效节点。

3.遍历链表，标记有效结点的flag为true，同时计数有效结点的个数。

4.对结点进行排序，排序原则：按flag从大到小排序(把有效结点即flag为1的结点排到前面，把无效结点即flag为0的结点排到后面)，否则flag为同值的话，按数据域data升序排序。

5.前面已经排序完毕，按要求输出即可。
